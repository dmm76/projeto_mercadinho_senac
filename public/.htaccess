# /public/.htaccess
Options -Indexes
Options -MultiViews
RewriteEngine On

# Arquivos/diretórios reais: entrega direto
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Demais requests: front controller
RewriteRule ^ index.php [QSA,L]
